/**
 * @type {Object}
 * format:
 *  %library_name%: {
 *      %alias%: '%real_method%'
 *  }
 */

module.exports = {
    lodash: {
        each: 'forEach',
        eachRight: 'forEachRight',
        extend: 'assignIn',
        extendWith: 'assignInWith',
        first: 'head'
    }
};
